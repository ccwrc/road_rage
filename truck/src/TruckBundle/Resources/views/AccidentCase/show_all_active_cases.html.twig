
Active cases: <br/>

{% for case in cases %}
    <a title="{{ case.vehicle.companyName }}" href="{{ path('truck_operator_panel', {'caseId': case.id}) }}"> {{ case.id }} </a> <br/>
{% else %}
    Empty.
{% endfor %}
