
<a title="switch to inactive cases" class="casesSwitch" 
   href="{{ path('truck_operator_panel', {'caseId': "0", 'casesStatus': "inactive"}) }}">
    Active cases:
</a>
<br/>

{% for case in cases %} 

    {% if caseId == case.id %}
        <button title="{{ case.vehicle.companyName }}" class="caseButton" 
                style="background-color:{{ case.progressColor }}"
                onclick="window.location = '{{ path('truck_operator_panel', {'caseId': case.id}) }}';">
            &rarr; {{ case.id }} &larr;
        </button> <br/>
    {% else %}
        <button title="{{ case.vehicle.companyName }}" class="caseButton" 
                style="background-color:{{ case.progressColor }}"
                onclick="window.location = '{{ path('truck_operator_panel', {'caseId': case.id}) }}';">
            {{ case.id }}
        </button> <br/>
    {% endif %}

{% else %}
    Empty.
{% endfor %}

<div class="navigation">
    {{ knp_pagination_render(cases) }}
</div>





